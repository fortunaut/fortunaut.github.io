
const NotFoundPage = () => {
  return <>
    <h2>Page Not Found</h2>
    <p>
      This address is not recognized. Use the links at the top of the page to navigate to a more useful page!
    </p>
  </>;
};

export default NotFoundPage;